# USER.md

Arquivo opcional de contexto humano do workspace.

Fonte canonica:

- `AGENTS.md` para as regras de startup e uso desta memoria.
- `MEMORY.md` e `memory/` para fatos curados e contexto recente do trabalho.

Use este arquivo apenas quando houver preferencia, limite ou contexto pessoal duravel que realmente melhore a execucao. No estado atual, ele pode permanecer enxuto.
